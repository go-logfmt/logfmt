language: go
sudo: false
go:
  - 1.2
  - 1.3
  - 1.4
  - 1.5
  - release
  - tip

before_install:
  - go get github.com/mattn/goveralls
  - go get golang.org/x/tools/cmd/cover

before_script:
  - go get -v -t ./...

script:
  - goveralls -service=travis-ci
